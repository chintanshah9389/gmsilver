# Copy to keystore.properties (gitignored) and fill real values.
# Generate a keystore first, then keep a secure backup of BOTH the .keystore and this file.
# Losing them means you cannot update the same Play Store app.

storeFile=gmsilver-release.keystore
storePassword=REPLACE_ME
keyAlias=gmsilver
keyPassword=REPLACE_ME
